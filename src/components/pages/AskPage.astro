---
/**
 * Tab — Ask the Research. An LLM chat grounded in the compiled research, with a
 * graceful fallback when the serverless endpoint isn't available.
 */
import type { Locale } from '../../lib/i18n';
import AskWidget from '../AskWidget.astro';

interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
const es = locale === 'es';
---

<div class="container-narrow tab-body stack">
  <header>
    <h1>{es ? '¿Tenés una pregunta?' : 'Got a question?'}</h1>
    <p class="lead">
      {
        es
          ? 'Preguntale a un asistente que responde únicamente con la investigación compilada de este sitio. Si no puede responder, anota la pregunta para una próxima actualización.'
          : 'Ask an assistant that answers only from this site’s compiled research. If it can’t answer, it notes your question for a future update.'
      }
    </p>
  </header>

  <AskWidget locale={locale} />
</div>

<style>
  .tab-body {
    padding-block: var(--s-6) var(--s-7);
  }
  .lead {
    font-size: var(--step-1);
  }
</style>
