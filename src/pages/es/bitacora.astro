---
import Base from '../../layouts/Base.astro';
import LogPage from '../../components/pages/LogPage.astro';
import { section } from '../../lib/content';
const locale = 'es' as const;
const meta = await section('log-intro', locale);
---

<Base locale={locale} tab="log" title={meta.headline} description={meta.short}>
  <LogPage locale={locale} />
</Base>
