# build output
dist/
.astro/

# dependencies
node_modules/

# generated video output
video/out/

# generated at build from the content layer (rebuilt by `pnpm build`)
functions/api/corpus.json

# logs / os
*.log
.DS_Store
.env
.env.*
!.env.example

# direnv + wrangler local secrets & temp
.direnv/
.dev.vars
.wrangler/

# transient hand-off from `update` research step → `log` step (consumed each run)
.update-pending-log.json

# transient prose-linter artifacts: metrics baseline (verify delta) + guidance the
# prose step reads. Regenerated every pipeline run by scripts/lint-prose.mjs.
.prose-lint.json
.prose-lint.md
